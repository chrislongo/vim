# vim / neovim config

## Installation

Optionally, back up existing config:

    mv ~/.config/nvim ~/.config/nvim.bak 2>/dev/null

Clone the repo:

    git clone https://github.com/chrislongo/vim.git ~/.config/nvim

Open Neovim — lazy.nvim will install itself and all plugins automatically on first launch:

    nvim
